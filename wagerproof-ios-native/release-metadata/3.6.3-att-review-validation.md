# iOS 3.6.3 review correction

## Scope

- Keep the approved ATT explanation and preview. Both the preview and Continue invoke the same system authorization handler.
- Continue advances only after a resolved authorization status. Denied and restricted allow progress; inactive or unresolved requests remain retryable.
- Completed accounts with undetermined device permission see the same explanation before the main tabs or post-onboarding paywall.
- Meta automatic logging and advertiser identifier collection default off. Meta SDK initialization, events, and matching require authorized ATT. RevenueCat billing remains available, but advertising identifiers and matching attributes require authorization; previous matching attributes are cleared otherwise.
- `asc_description.txt` was saved and read back exactly in App Store Connect for app 6757089957, iOS 3.6.3, English (U.S.), on 2026-09-23. The revised feature-focused copy is 3693 characters and groups free access, Premium, Premium Plus, Pro, and purchase requirements in one section near the bottom, per the requested copy revision. No screenshots, promotional text, or review submission state were changed.

## Validation

- Xcode simulator build passed.
- Five focused tests passed: no automatic request on presentation, all resolved choices allow progress, inactive/interrupted retry, existing choice and Settings revocation, concurrent-tap suppression.
- Existing iPhone 17 Pro Max simulator D69B4254-398D-4806-921B-56411888C597, iOS 26.0:
  - Reset app permissions and launch onboarding step 14: explanation remains visible without a native prompt.
  - Tap Continue: native ATT appears. Choose Ask App Not to Track: advances to sports selection.
  - Reset app permissions and launch the DEBUG `returningTracking` harness: the real RootView is given a completed-onboarding account and shows the explanation.
  - Tap the preview: native ATT appears. Choose Allow: main tabs appear.

## Remaining release checks

- App Store Connect still has rejected build 3.6.3 (457) selected. These code changes require a new build before resubmission.
- Capture the new build on a physical iPad running the review OS. A simulator result does not establish iPadOS 27 behavior.
- Add the physical-device recording and exact navigation steps to Review Notes before resubmitting. The previous September 21 recording exists in the review conversation; the current Notes field contains no ATT recording link.
- Verify RevenueCat's WagerProof Meta integration has `Send events when ATT consent is not authorized` disabled. The connected API can access WagerProof but exposes no Meta integration settings; the current browser login only lists other projects. Client consent gating does not verify server integration policy.

## Physical-device verification steps

1. Use a fresh installation or reset device tracking permissions. Allow apps to request tracking in Settings; an already denied or restricted device will not display another request.
2. Test a new onboarding account and an account whose onboarding is already complete.
3. Show the explanation, tap Continue or the preview, and record the native ATT dialog.
4. Test both Allow and Ask App Not to Track. Verify the app remains usable and no advertising identifiers or matching data are sent without authorization.
5. Record the build and OS version and add the video to the review response and Review Notes.
