import TieredPlansPage from '@/features/tieredPaywall/TieredPlansPage';

/** Exercise the same plans, prices, and checkout as the customer-facing page. */
export default function PaywallTest() {
  return <TieredPlansPage />;
}
