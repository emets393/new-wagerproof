<?xml version="1.0" encoding="utf-8"?>
<!--
  Provider config for the Top Outliers widget. Sizes cover iOS small
  (~2x2), medium (~4x2) and large (~4x4). updatePeriodMillis = 60 min to
  match the iOS timeline `.after(now + 60 min)` refresh policy; the app also
  force-refreshes on foreground/sign-in via the widget sync path.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_top_outliers_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="3600000"
    android:widgetCategory="home_screen" />
