# Agents Feature Inventory — Part 1: Root Screens

> Parity contract for the Android (Jetpack Compose) port. Source: `wagerproof-ios-native/Wagerproof/Features/Agents/`.
> Covers: AgentsView, AgentsView+TopPicks, AgentDetailView, PublicAgentDetailView, AgentSettingsView, AgentStatsView.

### `AgentsView.swift` (~1037 lines)
- **Purpose & nav context**: Agents tab root landing screen (ports RN `AgentsHubScreen` at `app/(drawer)/(tabs)/agents/index.tsx`). Owns a `NavigationStack(path: $navPath)` of `NavigationPath`; all `AgentsRoute` cases resolve here via `.navigationDestination(for: AgentsRoute.self)`. Not itself reached by a route — it IS the tab. Also consumes Search-tab deep links via `tabStore.pendingAgentRoute` (`.onChange(of:, initial: true)` → `consumePendingAgentRoute()`, which clears the flag then pushes `.publicAgentDetail` if `route.isPublic` else `.agentDetail`).
- **Layout**: Background `Color.appSurface.ignoresSafeArea()`. `.navigationTitle("Agents")`, `.navigationBarTitleDisplayMode(.large)` (large collapsing title matching Games/Props/Outliers). Toolbar: leading `WagerProofLeadingToolbarItem()` (brand wordmark, passive); trailing group = `createAgentToolbarButton` ("+" plus glyph, size 17 semibold, tint `Color.appTextPrimary`; when limit hit → disabled `lock.fill` glyph size 15, tint `appTextSecondary`, a11y "Agent limit reached") then `SettingsToolbarButton(tabStore:)` (gear, pushes Settings via `.wagerProofSettingsDestination`). Body = `tabbedContent`, branched by private enum `AgentsTab { myAgents, leaderboard, topPicks }` (labels "My Agents"/"Leaderboard"/"Top Picks"). The **tab picker** (`tabPicker`) is a segmented `Picker` clipped to capsule + a trailing `filterMenu` button (`line.3.horizontal.decrease` glyph 15 semibold in 32×32 frame, tint appTextPrimary, a11y "Filter"), wrapped in `LiquidGlassCapsule()` modifier with h-padding 4/v 4 inner, 14/8 outer. Critically it is rendered as a **pinned section header inside each branch's scroll content** (never a toolbar/safeAreaInset — that would cover the large title). My Agents branch order (a `List`, `.plain` style, `listRowSpacing(10)`, `scrollContentBackground(.hidden)`, `defaultMinListRowHeight 1`, all row backgrounds clear / separators hidden): (1) pinned header = tabPicker (clear listRowBackground, `.textCase(nil)`); (2) `officeRow` hero — `PixelOffice(agents:isActive:true)` SpriteKit scene with two corner overlays: top-leading `agentHQStatusPill` (6pt green dot `Color(hex: 0x22C55E)` + "Agent HQ — Live"/"Night Shift" text 11 bold tracking 0.3 white, h-pad 11/v 6, `.liquidGlassBackground(in: Capsule())`), top-trailing `AgencyStatsPill(agents:)`; both padded 10; row insets 4/12/4/12; (3) `sortRow` — right-aligned non-interactive `sortIndicator` (arrow.up.arrow.down 10 bold + sort label 11 semibold, `Color.appTextMuted`, no capsule); (4) `AgentRowCard` per agent; (5) trailing `Color.clear` 96pt spacer for the floating tab bar. Day/night for the pill resolves from `@AppStorage("pixel-office-time-mode")` ("day"/"night"/"auto" → night if hour ≥19 or <6).
- **Stores & bindings**: `@Environment`: `AuthStore` (reads `auth.phase` → `currentUserId` lowercased uuid), `ProAccessStore`, `MainTabStore` (`tabStore.pendingAgentRoute` read/cleared; passed to Settings/chat destinations), `SettingsStore`, `RevenueCatStore`, `AdminModeStore` (last three only re-injected into the Settings destination). `@State store = AgentsStore()` — reads `.loadState`, `.agents`, `.activeCount`, `.totalCount`; calls `.bind(userId:)`, `.refresh()`, `.setActive(agentId:isActive:)`, `.setAutoGenerate(agentId:autoGenerate:)`, `.delete(agentId:)`. `@State leaderboardStore = LeaderboardStore()` — bindables `.sortMode` (`SortMode.allCases`), `.timeframe` (`Timeframe.allCases`), `.excludeUnder10Picks`. `@State topPicksStore = TopAgentPicksFeedStore()` — `.filterMode` (`FilterMode.allCases` — Top 10/Following/Favorites), `.loadState`, `.bind(viewerUserId:)`, `.refresh()`. `@State topPicksFavorites = FavoriteAgentsStore()` (injected into feed env). Computed `entitlements = AgentEntitlementsStore(proAccess:)` rebuilt per render, injected into env for children; reads `.canCreateAnotherAgent(activeCount:totalCount:)`, static `.maxConcurrentActiveAgents` (8-active cap). `AgentPicksSeenStore.hasUnread(agentId:latestActivity:)` (device-local UserDefaults ledger; `@State unreadRefreshToken` bumped `onAppear` to force recompute). `@AppStorage("agents-pinned-ids")` comma-joined pinned UUID set.
- **States**: My Agents: `.idle/.loading` with empty agents → `myAgentsSkeleton` (`AgentHQShimmer` + 4× `AgentRowCardShimmer`, opacity transitions, in `pickerHeaderScroll`); loading with cached agents → populated list; `.loaded` empty → `emptyState`: "Your AI Picks Expert" (24 heavy) / "Build a virtual analyst that thinks the way you bet." (15) + three `journeyStep` cards (icon in 40pt circle of `Color(hex: 0x00E676).opacity(0.1)`, 14pt corner card fill `appBorder.opacity(0.25)` + green 0.12 stroke): "Build Your Strategy" (slider.horizontal.3), "AI Analyzes Every Game" (brain.head.profile), "Get Daily Picks" (bolt.fill), then `.borderedProminent` green `Color(hex: 0x00E676)` CTA "Create Your First Agent" (plus icon) → pushes `.createAgent`; `.failed(msg)` → `ContentUnavailableView` "Couldn't load agents" (exclamationmark.triangle) + msg + green "Retry" (arrow.clockwise) calling `store.refresh()`. All non-List states wrap in `pickerHeaderScroll` (ScrollView + LazyVStack pinned section header = tabPicker).
- **Interactions**: Tab picker selection (haptic `.sensoryFeedback(.selection, trigger: selectedTab)`). Filter menu is contextual: My Agents → sort `Picker` over `AgentSortOption { winRate("Win %","percent"), units("Units","dollarsign.circle"), streak("Streak","flame"), name("Name","textformat"), newest("Newest","clock") }`; Leaderboard → Sort picker + Timeframe picker + "10+ picks only" toggle (bound straight into `leaderboardStore`); Top Picks → "Show" picker on `topPicksStore.filterMode`. Card tap → push `.agentDetail(agentId:)`; card long-press → sets `pendingLongPressAgent` (confirmation dialog). Leading swipe (no full swipe) per row: Pause/Activate (`pause.circle.fill`/`play.circle.fill`, tint `Color(hex: 0xF97316)` when active else `Color.appWin`) → `toggleActive` (activation blocked at 8 active → `activeCapAlert`); Pin/Unpin (`pin.fill`/`pin.slash.fill`, tint `0xEAB308`) → `togglePin`; Edit (`slider.horizontal.3`, tint `appAccentBlue`) → push `.editAgent`; Details (`info.circle.fill`, tint `0x6366F1`) → push `.agentDetail`. Trailing swipe (full-swipe allowed, destructive): Delete (`trash.fill`, tint `appLoss`) → sets `pendingDeleteId`. Pull-to-refresh → `store.refresh()`. "+" toolbar tap → push `.createAgent` with `.sensoryFeedback(.impact(weight: .medium), trigger: navPath.count)`. Leaderboard row tap → push `.publicAgentDetail(agentId: entry.avatarId)`. Top Picks agent tap AND pick tap → push `.publicAgentDetail(agentId:)` (no public pick sheet yet). Sorting: `sortedAgents` = sort by option (win% computed wins/(wins+losses), unsettled sorts last as -1; units = `performance?.netUnits ?? 0`; streak = `currentStreak ?? 0`; name = localized case-insensitive; newest = `createdAt` desc) then stable-partitions pinned ids first.
- **Sheets/dialogs/alerts**: `confirmationDialog` (title = agent name, visible) presented by derived binding on `pendingLongPressAgent != nil`; actions: "Settings" (pushes `.agentDetail` — note: not editAgent), "Turn Autopilot On/Off" (`store.setAutoGenerate`), destructive "Delete Agent" (→ `pendingDeleteId`), "Cancel". `.alert("Delete agent?")` on `pendingDeleteId != nil` presenting the agent name: destructive "Delete" → `performDelete(id)`, "Cancel"; message: "“{name}” and its picks will be permanently removed. This can't be undone." `.alert("Active limit reached", isPresented: $activeCapAlert)`: OK; message "You can have up to 8 agents active at once. Pause one to activate another."
- **Animations**: `.staggeredAppear(index:)` cascade on each `AgentRowCard`; `.animation(.spring(response: 0.45, dampingFraction: 0.82))` keyed on `sortOption` and on `pinnedIdsRaw` (cards slide on re-sort/pin); skeleton→content opacity transitions; `.shimmering()` sweep on both shimmer components.
- **Routes pushed**: `AgentsRoute.agentDetail(agentId:)` → `AgentsRouterB15.ownerDetail(agentId:prefetched:)` (prefetches the row from store for instant header paint); `.createAgent` → `AgentCreationView()` with `store` + `entitlements` env; `.publicAgentDetail(agentId:)` → `AgentsRouterB15.publicDetail(agentId:)`; `.editAgent(agentId:)` → `AgentSettingsView(agentId:initialAgent:)`. Plus `.wagerProofSettingsDestination` / `.wagerProofChatDestination` pushes. DEBUG: `-agentsTab myAgents|leaderboard|topPicks` launch arg forces initial tab; DEBUG init injecting a store.
- **Private sub-views / types**: `AgentSortOption` (private enum, sort labels/icons); `AgentsTab` (private enum, inner tabs); `AgentHQShimmer` (internal struct — skeleton plate for the office hero: `appSkeleton.opacity(0.55)` 20pt-radius rounded rect at `PixelOfficeGeo.mapWidth/mapHeight` aspect (864:800), with two `SkeletonCapsule` pills 112×20 top-leading / 92×20 top-trailing mirroring the corner pills); `AgentRowCardShimmer` (internal — reproduces AgentRowCard chrome: 26pt continuous rounded rect, `.ultraThinMaterial` at opacity 0.55 in dark mode else 1, 0.5pt `appBorder.opacity(0.4)` stroke, shadow black 0.06/radius 4/y 2; placeholder layout: 52×52 avatar block r14, 120×14 name, 56×16+44×16 chips, 58×16 streak capsule over six 8pt-wide W/L bars heights [14,22,10,26,18,12] in a 28pt-tall bottom-aligned row within a 96pt column, divider, 2× 40×18 sport pills + 88×12 record; shimmer applies to content only, chrome stays solid). Gating: create button gated on `entitlements.canCreateAnotherAgent`; 8-active cap on activation. Haptics: selection on tab change, medium impact on "+".

### `AgentsView+TopPicks.swift` (~104 lines)
- **Purpose & nav context**: B16 integration shim, deliberately in its own file (B14/B15 co-edit note). Contains one reusable view, `TopAgentPicksFeedContainer` — a drop-in wrapper for the Top Picks feed usable from any surface (drawer screen, deep link) without redoing binding plumbing. **Note**: `AgentsView` itself no longer uses it (it lifts `TopAgentPicksFeedStore`/`FavoriteAgentsStore` into its own `@State` so the bar's filter menu can drive `filterMode`); the container remains as the canonical standalone wiring. Roughly 45 of its lines are a doc-comment describing the (now-applied) integration steps and referencing ticket `docs/wagerproof-migration/tickets/081-agents-top-picks-feed-wiring.md` and FIDELITY-WAIVER #070 (resolved).
- **Layout**: Body is just `TopAgentPicksFeed(store:onAgentTap:onPickTap:)` with `favorites` injected via `.environment(...)` — no chrome of its own.
- **Stores & bindings**: `@State store: TopAgentPicksFeedStore` (created in init; DEBUG init accepts injected store/favorites), `@State favorites: FavoriteAgentsStore`. Reads `store.loadState`; calls `store.bind(viewerUserId:)`, `store.refresh()`. Props: `viewerUserId: String?`, `onAgentTap: (String) -> Void`, `onPickTap: (TopAgentPickFeedRow) -> Void` (row exposes `.avatarId`). Underlying RPC: `get_top_agent_picks_feed_v2` (per AgentsView docs).
- **States**: Delegates all loading/empty/error to `TopAgentPicksFeed`; only behavior here is first-refresh-if-idle in `.task` and rebind+refresh in `.onChange(of: viewerUserId)`.
- **Interactions / sheets / animations / routes**: None locally — taps forwarded through the two closures (in AgentsView both route to `AgentsRoute.publicAgentDetail`).

### `AgentDetailView.swift` (~637 lines)
- **Purpose & nav context**: Owner agent detail (ports RN `app/(drawer)/(tabs)/agents/[id]/index.tsx`). Pushed via `AgentsRoute.agentDetail(agentId:)` through `AgentsRouterB15.ownerDetail(agentId:prefetched:)`. Params: `agentId: String`, `initialAgent: AgentWithPerformance?` (prefetched row for instant header paint).
- **Layout**: `CollapsingWidgetScroll(heroMaxHeight: 196, heroMinHeight: 60)` with three slots: background = `AgentPixelWaveBackground(avatarColor: agent?.avatarColor ?? "#6366f1", progress:, rippleEmitter:)` (animated near-black pixelwave field, full bleed); hero = `AgentGlassHero(agent:performance:lockedNetUnits: !canSeePicks, progress:, isGenerating: store.isGenerating, onAvatarTap: rippleAvatar)` padded 16 horizontal / 6 top (falls back to a top-padded `ProgressView` while agent is nil); content sections IN ORDER: (1) `picksSection` ("Today's Picks"), (2) `performanceSection`, (3) `recentActivitySection`, (4) `pickHistorySlot`. Each section is **container-less/inline** (no glass card) under an `AgentSectionHeader` (defined in this file: 11pt-bold SF icon + uppercased `.footnote.weight(.semibold)` title, `.secondary` color, no internal h-padding, flush at `WidgetCard.hInset`); section gap constant 28pt (`sectionGap`, wider than the standard `WidgetCard.gap` 12). Page-level chrome: `.preferredColorScheme(.dark)` (always-dark), `.toolbarBackground(.hidden, for: .navigationBar)` (aura glows to top), `.toolbar(.hidden, for: .tabBar)`, nav bar fully hidden while the focus overlay is up (`focusStartIndex != nil`), empty `.navigationTitle("")` inline (hero carries the name). Toolbar trailing: gear `NavigationLink` → `AgentSettingsView(agentId:, initialAgent: store.snapshot?.agent)`, `.tint(.white)`. Section headers use icons: checklist / chart.line.uptrend.xyaxis / clock (inside AgentTimeline) and the pick-history manila folder card.
- **Stores & bindings**: `@Environment(AuthStore)` (`auth.phase` → `currentUserId`), `@Environment(ProAccessStore)`; computed `entitlements = AgentEntitlementsStore(proAccess:)` — reads `.canViewAgentPicks`, `.canUseAutopilot`, `.isAdmin`. `@State store = AgentDetailStore(agentId:)` — reads `.snapshot` (`.agent`, `.performance`), `.snapshotLoadState`, `.todaysPicks`, `.todaysParlays`, `.todaysBetItems`, `.performancePicks`, `.performanceParlays`, `.performanceLoadState`, `.historyLoadState`, `.fullBetHistory`, `.todaysGenerationRun` (`.picksGenerated`, `.noGames`, `.weakSlate`, `.slateNote`), `.isGenerating`, `.liveRunState`, `.lastGenerationError`; calls `.refreshSnapshot()`, `.loadHistory(isOwner:)`, `.loadPerformancePicks(isOwner:)`, `.resumeActiveGenerationIfNeeded()`, `.generatePicks()`, `.regenerationsRemaining()` (max daily 3), `.setAutoGenerate(_:)`, `.setAutoGenerateTime(_:timezone:)`. `@State auditStore = AgentPickAuditStore()` (`.isPresented`, `.selectedPick`, `.payload`, `.present(pick:)`). `AgentPicksSeenStore.hasUnread/markSeen` (device-local read receipts). `AgentColorPalette.primary(for: avatarColor)` → `agentTint` (fallback `Color(hex: 0x00E676)`). `@State rippleEmitter = GlyphRippleEmitter()`.
- **States**: Picks section — locked (`!canSeePicks`) → `AgentLockedPicksRail(accent:)` edge-bleeding past `WidgetCard.hInset`; first load (snapshot loading + empty picks) → `AgentTodaysPicksRailSkeleton()`; otherwise a top-anchored ZStack crossfade between `picksRail` (has picks & not generating: `AgentTodaysPicksRail` horizontal mini-ticket rail, edge-bled, + `generateFooter`) and `generationCard` (`AgentGenerationCard(spriteIndex:accent:state: store.liveRunState, isGenerating:canGenerate:lockedLabel:conclusion:onGenerate:)` — one card morphing idle "research/control room" tile → live polling run: avatar shrinks to corner, glyph speeds up + oranges, verbs track current tool, tool skeletons stack; a zero-pick run surfaces `noPicksConclusion` green console text: "No games were available in this agent's preferred sports today." / "This agent skipped today because the slate was too weak for its settings." / `run.slateNote` / "The agent completed its analysis and passed on the slate."). Generation label: "Generate Picks Locked" (not entitled) / "Daily limit reached" (0 remaining) / "Generate Today's Picks". Performance section — locked → lock.fill 26pt + "Upgrade to view pick charts" (14 semibold, appTextSecondary, 28 v-pad); `isPerformanceSettling` (empty + idle/loading — never a spinner or empty flash) → `AgentPerformanceChartSkeleton()`; loaded → `AgentPerformanceCharts(items: picks+parlays as AgentBetItem, preferredSports:, agentColor:, showsTitle: false)` (Apple Charts; own empty state for loaded-but-empty). Recent Activity — `AgentTimeline(agent:performance:todaysPicks:todaysParlays:todaysRun:)` self-hides when empty (whole section vanishes for new agents). Pick History — `AgentPickFolderCard(recentItems: canSeePicks ? fullBetHistory : [], totalCount:, loading: canSeePicks && isHistoryLoading && empty, locked: !canSeePicks, agentColor:, onTap:)` — manila folder embossed "PICK HISTORY" with recent tickets poking out. Error → generic `.alert("Error")`.
- **Interactions**: Pull-to-refresh → `refresh()` (snapshot + parallel history/performance loads when entitled). `.task(id: historyReloadKey)` where key = `"{agentId}-{canSeePicks}-{isOwnAgent}-{userId}"` → snapshot-if-nil, parallel history+performance, `resumeActiveGenerationIfNeeded()`, then `maybeAutoplayUnreadPicks()`. `.onAppear` (when snapshot exists) re-syncs snapshot + resumes generation (Settings saves through its own store, so this heals staleness). Mini-ticket tap (pick or parlay) → sets `focusStartIndex` to that item's index in `todaysBetItems` with `focusPrintIntro = false` (opens full-screen pager at that card; audit reachable from focused card's "View data audit"). `AutoPilotControlButton` chip tap → `showAutoPilotSheet = true`; `RegenerateControlButton` chip (shows remaining runs, disabled per `canRegenerate`) → `showRegenSheet = true`. Generation swipe/tap inside the card → `runGeneration()`: awaits `store.generatePicks()`; on success with fresh items → snapshot into `lastGenerationResultItems`, `focusPrintIntro = true`, `focusStartIndex = 0` (printer reveal), then `markPicksSeen()`; on failure → `errorMessage = store.lastGenerationError`. Hero avatar tap → `rippleAvatar(at:)`: light `UIImpactFeedbackGenerator` haptic + `rippleEmitter.emit(at:)` glyph ripple through the pixelwave (easter egg). Toolbar gear → push Settings.
- **Sheets/dialogs/alerts**: `.sheet(isPresented: $auditStore.isPresented)` → `AgentPickPayloadAuditSheet(pick:payload:)` (defined in this file: NavigationStack + ScrollView around `AgentPickPayloadAuditWidget`, background `Color(hex: 0x0B1011)`, title "Pick Audit" inline, `.presentationDetents([.large])`). `.sheet($showHistorySheet)` → `PickHistorySheet(items: store.fullBetHistory, agentName:, agentColor:)` (full rolodex + result/sport/sort filters). `.sheet($showRegenSheet)` → `RegenerateBottomSheet(remaining:, maxDaily: 3, accent:, canRegenerate:, onRequest:)` — onRequest dismisses then runs generation. `.sheet($showAutoPilotSheet)` → `AutoPilotBottomSheet(agentName:, accent:, canUseAutopilot:, remaining:, maxDaily: 3, initialAutoOn: agent?.autoGenerate ?? false, initialTime: "09:00" default, initialTimezone: "America/New_York" default, recentRuns:, onSetAuto:, onSaveTime:)` — `recentRuns` derived via `AgentRunSummaryRow.derive(picks: dedup(performancePicks + todaysPicks), todaysRun:, todayStr:)`. `.alert("Error", isPresented: errorAlertBinding, presenting: errorMessage)` OK-dismiss. Full-screen `.overlay` (not a sheet): `AgentPickFocusView(items: focusItems, accent:, startIndex:, printIntro: focusPrintIntro, onAudit:, onClose: { focusStartIndex = nil })` with `.transition(.opacity)`; `focusItems = focusPrintIntro ? lastGenerationResultItems : store.todaysBetItems`.
- **Animations**: Collapsing hero driven by scroll `progress` (196→60pt). Rail↔generation-card crossfade: `.animation(.easeInOut(duration: 0.3), value: showsPicksRail)` scoped to the ZStack slot only (deliberately keyed on `showsPicksRail = !todaysBetItems.isEmpty && !isGenerating`, not `isGenerating`, so the in-card idle→polling morph stays owned by `AgentGenerationCard`). Performance skeleton→chart crossfade `.easeOut(duration: 0.25)` keyed on `isPerformanceSettling`, both branches `.transition(.opacity)`. Printer cinematic: fresh/unseen picks open the focus overlay with `printIntro: true` (printer feed + card fan-out). `maybeAutoplayUnreadPicks()` — if `AgentPicksSeenStore.hasUnread` and not generating and overlay not up, autoplay the printer reveal for autopilot/finished-while-away picks, then mark seen (`markSeen(agentId:, upTo: max(newest item createdAt, agent.lastGeneratedAt))` clears the list's unread dot).
- **Ownership/premium gating**: `canViewPicks = entitlements.canViewAgentPicks` (Pro); `isOwnAgent` (userId == agent.userId lowercased); `canSeePicks = canViewPicks || isOwnAgent` (owners always see their own picks/charts, web parity); `canRegenerate` = admin override when not entitled, else `regenerationsRemaining() > 0`. Haptics: light impact on avatar ripple.
- **Routes pushed**: Only the toolbar `NavigationLink` → `AgentSettingsView` (no AgentsRoute pushes; overlay/sheets are local).
- **Sub-views in file**: `AgentSectionHeader` (shared inline section header, also used by PublicAgentDetailView), `AgentPickPayloadAuditSheet` (audit modal wrapper).

### `PublicAgentDetailView.swift` (~449 lines)
- **Purpose & nav context**: Read-only public agent detail with Follow/Unfollow CTA (ports RN `app/(drawer)/(tabs)/agents/public/[id].tsx`). Pushed via `AgentsRoute.publicAgentDetail(agentId:)` through `AgentsRouterB15.publicDetail(agentId:)`. Param: `agentId: String` (no prefetch).
- **Layout**: Mirrors `AgentDetailView` exactly: same `CollapsingWidgetScroll(heroMaxHeight: 196, heroMinHeight: 60)` + `AgentPixelWaveBackground` + `AgentGlassHero` (here with `subtitleSystemImage: "globe"`, `subtitle: "Public Agent"`, no `isGenerating`), same `sectionGap` 28. Content order: (1) `followBlock` (h-pad 16, bottom `WidgetCard.gap`), (2) `picksSection` (Today's Picks rail, read-only), (3) `performanceSection`, (4) `recentActivitySection`, (5) `pickHistorySlot`, (6) `disclaimer` (info.circle 12pt + "AI agents analyze data — they do not constitute betting advice. Verify independently and wager responsibly." 11pt appTextSecondary, whole row opacity 0.7; h-pad 16 / bottom 12). Chrome: `.preferredColorScheme(.dark)`, hidden toolbar background, hidden tab bar, nav bar hidden while the focus overlay is up, `.navigationTitle(agent?.name ?? "Public Agent")` **inline** (unlike owner's empty title). Differences vs owner: no gear/settings, no generation card, no autopilot/regenerate footer, adds follow CTA + disclaimer + explicit empty-picks state.
- **Stores & bindings**: `@Environment(AuthStore)`, `@Environment(ProAccessStore)`; `entitlements = AgentEntitlementsStore(proAccess:)` (reads `.canViewAgentPicks`). `@State store = AgentDetailStore(agentId:)` — same reads as owner plus `.isFollowingFromSnapshot`; calls `.refreshSnapshot()`, `.loadHistory(isOwner:)`, `.loadPerformancePicks(isOwner:)`. `@State auditStore = AgentPickAuditStore()`. Service call: `AgentChatService.setFollow(userId:agentId:follow:)`. `@State`: `isFollowing`, `followBusy`, `showHistorySheet`, `errorMessage`, `focusStartIndex` (no `focusPrintIntro` — public viewers never trigger a run), `rippleEmitter = GlyphRippleEmitter()`.
- **States**: Top-level: agent present → `detailScroll`; else `.loading` snapshot → centered `ProgressView`; else `notFoundView` ("Agent not found" 15 semibold under `person.crop.circle.badge.exclamationmark` 36pt, appTextSecondary). Picks section: locked → `AgentLockedPicksRail`; loading+empty → `AgentTodaysPicksRailSkeleton`; loaded empty → `emptyPicksState` (`calendar.badge.exclamationmark` 28pt + "No picks yet today" 14 semibold appTextSecondary, 22 v-pad); else `AgentTodaysPicksRail` (edge-bled). Performance: same locked / `isPerformanceSettling` skeleton / `AgentPerformanceCharts` triad as owner (identical `isPerformanceSettling` logic). Recent Activity: `AgentTimeline`, self-hides. Pick History: same `AgentPickFolderCard` params as owner. Follow states: `followBusy` → white `ProgressView` inside the button.
- **Interactions**: `.task(id: historyReloadKey)` (same key formula as owner) → snapshot-if-nil, seed `isFollowing` from `store.isFollowingFromSnapshot`, parallel history+performance when entitled. Pull-to-refresh → `refresh()` (snapshot + loads + re-seed isFollowing). Follow button tap → `toggleFollow()`: guards signed-in (else `errorMessage = "Sign in to follow agents."`), optimistic flip of `isFollowing`, `AgentChatService.setFollow`, rollback + NSError localizedDescription on failure; `followBusy` disables during flight. Ticket/parlay tap → `focusStartIndex = index` (focus pager, `printIntro: false`, share included for parlays). Folder tap → `showHistorySheet = true`. Hero avatar tap → `rippleAvatar` (light impact haptic + `rippleEmitter.emit`).
- **Sheets/dialogs/alerts**: `.sheet($auditStore.isPresented)` → `AgentPickPayloadAuditSheet` (audit reachable from a focused card's onAudit); `.sheet($showHistorySheet)` → `PickHistorySheet(items:agentName:agentColor:)`; `.alert("Error", errorAlertBinding, presenting: errorMessage)` OK; `.overlay` `AgentPickFocusView(items: store.todaysBetItems, accent:, startIndex:, printIntro: false, onAudit:, onClose:)` with `.transition(.opacity)`.
- **Ownership gating**: `isOwnAgent` (userId match) → `ownAgentBanner` replaces the follow button: `person.crop.circle.badge.checkmark` + "This is your agent" (14 semibold) both `Color.appWin`, 12pt padding, 12pt-radius fill `appWin.opacity(0.1)`. `canSeePicks = entitlements.canViewAgentPicks || isOwnAgent` gates picks/performance/history identically to owner. Follow button visual: full-width, v-pad 14, 14pt-radius continuous rect — not-following: fill `Color(hex: 0x00E676)`, white text/plus icon; following: fill `appBorder.opacity(0.5)`, 2pt green `0x00E676` stroke, green text + checkmark; label 15 heavy "Follow"/"Following".
- **Animations**: Collapsing hero via progress; performance crossfade `.easeOut(0.25)` on `isPerformanceSettling`; focus overlay opacity transition; no print/printer choreography.
- **Routes pushed**: None — leaf screen (everything local sheets/overlay).

### `AgentSettingsView.swift` (~661 lines)
- **Purpose & nav context**: Owner-only full settings editor (ports RN `app/(drawer)/(tabs)/agents/[id]/settings.tsx`). Reached two ways: pushed from `AgentDetailView`'s toolbar gear NavigationLink, and via `AgentsRoute.editAgent(agentId:)` from the list's Edit swipe. Params: `agentId: String`, `initialAgent: Agent?`. Saves via `agent-authorized-action-v1` edge function, `update_agent` action (server-side Zod identical to RN). Full parity with the creation wizard — same `SliderInput`/`ToggleInput`/`OddsInput` primitives.
- **Layout**: Plain `Form` (grouped), `.navigationTitle("Settings")` inline. Toolbar trailing: Save button (bold "Save", `ProgressView` while saving, disabled unless `hasChanges && !saving`). Sections IN ORDER (grouped into `@ViewBuilder` clusters to stay under the 10-child cap): **Identity** — agent-name `TextField`; "Character" label (13 semibold) + horizontal ScrollView of the 8 pixel-office people (`PixelSpriteAvatar(spriteIndex: 0…7, animated: isSelected)` 42×56 in 10pt-radius tile, selected = fill `Color(hex: 0x00E676).opacity(0.18)` + 2pt green stroke, unselected fill `appBorder.opacity(0.3)`; a11y "Character N" + `.isSelected`) — replaces the old emoji grid though `emoji` still round-trips untouched for web/RN; "Color" label + adaptive `LazyVGrid` (min 44) of 10 swatches `["#3b82f6","#8b5cf6","#06b6d4","#10b981","#f59e0b","#ef4444","#ec4899","#6366f1","#14b8a6","#f97316"]`, 40pt circles, selected shows white bold checkmark + 3pt white ring. **Sports** — rows per `AgentSport.allCases` (sfSymbol tinted `appPrimary` in 24pt frame + label + trailing green checkmark when selected); tap toggles membership but removal blocked when it's the last sport (`sports.count > 1` guard); footer "Pick at least one sport this agent should cover." **Core Personality** — SliderInputs (5-step labeled): Risk Tolerance ["Very Safe","Conservative","Balanced","Aggressive","High Risk"], Underdog Lean ["Chalk Only","Prefer Favs","Balanced","Prefer Dogs","Dogs Only"], Over/Under Lean ["Unders Only","Prefer Under","Balanced","Prefer Over","Overs Only"], Confidence Threshold ["Any Edge","Low Bar","Moderate","High Bar","Very Picky"]; ToggleInput Chase Value ("Seek out bets where odds exceed model probability…"); SliderInput Parlay Appetite ["Straights Only","Rarely","Sometimes","Often","Loves Parlays"]; ToggleInput Parlays Only ("Force every play into multi-leg parlay tickets — the agent never submits straight picks"). **Bet Selection** — "Preferred Bet Type" segmented Picker [any/"Any", spread/"Spread", moneyline/"ML", total/"Total"]; Max Picks Per Day slider ["1 Pick"…"5 Picks"]; Skip Weak Slates toggle. **Data Trust** — Trust WagerProof Model + Trust Polymarket sliders (["Ignore","Low Trust","Moderate","High Trust","Full Trust"]); Polymarket Divergence Flag toggle. **Odds Limits** — `OddsInput` Max Favorite Odds (type .favorite) + Min Underdog Odds (type .underdog). **Conditional**: Football Settings (shown if sports ∩ {nfl,cfb}): Fade the Public toggle → nested Public Threshold slider ["55%","60%","65%","70%","75%"] (default idx 3) only when true; Weather Impacts Totals toggle → nested Weather Sensitivity slider ["Minimal","Low","Moderate","High","Maximum"] (default 3) when true; footer "Football-specific betting conditions". Basketball Settings (nba|ncaab): Trust Team Ratings slider (trust labels, default 3), Pace Affects Totals, Fade Back-to-Backs toggles. NBA Trends (nba only): Weight Recent Form slider ["Ignore","Light","Moderate","Heavy","Primary"] (default 3), Ride Hot Streaks / Fade Cold Streaks / Trust ATS Trends / Regress Luck toggles. Situational Factors (always): Home Court/Field Boost slider ["Ignore","Slight","Moderate","Strong","Maximum"]; Upset Alert toggle only when ncaab selected ("Flag potential March Madness upsets…"). **Custom Insights** — four `insightSection` Form sections: Betting Philosophy (book.fill, 500 max), Perceived Edges (chart.line.uptrend.xyaxis, 500), Situations to Avoid (xmark.octagon, 300), Target Situations (target, 300); each = multiline TextField "Add your notes…" (axis .vertical, lineLimit 3...8, 15pt), header icon turns green `0x00E676` when filled + a green "Filled" capsule badge (10 semibold on green 0.18 capsule), footer = description + right-aligned "count/max" (11pt, red `0xEF4444` when over limit — note: over-limit is only visual, save isn't blocked client-side). **Autopilot** — "Auto-generate picks" Toggle (green tint 0x00E676; setter intercepts: turning ON without `entitlements.canUseAutopilot` → `errorMessage = "Upgrade to Pro to enable autopilot."` and no change; whole toggle also `.disabled(!canUseAutopilot)`); when on, "Preferred time" row (monospaced 14 value "HH:mm TZ-abbr" via `tzAbbr` mapping New_York→ET, Chicago→CT, Denver→MT, Los_Angeles→PT else raw, chevron.right) → opens time picker sheet; footer switches: entitled "When on, the agent will analyze games and generate picks daily." vs "Pro subscribers get daily auto-generation. Free users can still manually generate." **Visibility** — "Public agent" Toggle (same green tint; ON gated on `entitlements.canCreatePublicAgent` → error "Only Pro users can make agents public."; disabled when not entitled); footer "Public agents appear on the leaderboard. Pick history and performance become visible." **Danger Zone** — "Delete Agent" `Label` with trash icon in `Color.appLoss`; footer "Deleting is permanent. Picks and performance history are lost."
- **Stores & bindings**: `@Environment(\.dismiss)`, `@Environment(ProAccessStore)`; `entitlements = AgentEntitlementsStore(proAccess:)` (`.canUseAutopilot`, `.canCreatePublicAgent`). `@State store = AgentDetailStore(agentId:)` — calls `.refreshSnapshot()`, `.saveSettings(payload:)`, `.delete()`; reads `.snapshot?.agent`, `.lastGenerationError` (reused as save-error text). Form state: `name`, `emoji` ("🤖" default), `spriteIndex` (0…7), `color` ("#3B82F6" default), `sports: Set<AgentSport>`, `personality: AgentPersonalityParams` (.default), `customInsights: AgentCustomInsights` (.empty), `autoGenerate` (true), `autoGenerateTime` ("09:00"), `autoGenerateTimezone` ("America/New_York"), `isPublic` (false), plus `hasChanges`, `saving`, `deleteConfirm`, `errorMessage`, `showTimePicker`. Every mutation flows through binding helpers (`intBind`/`optIntBind(default)`/`boolBind`/`optBoolBind`/`oddsBind`/`betTypeBind`/`insightBind`) that each set `hasChanges = true`; insight setter stores nil for empty string. Personality keypaths touched: riskTolerance, underdogLean, overUnderLean, confidenceThreshold, chaseValue, parlayAppetite, parlaysOnly, preferredBetType, maxPicksPerDay, skipWeakSlates, trustModel, trustPolymarket, polymarketDivergenceFlag, maxFavoriteOdds, minUnderdogOdds, fadePublic, publicThreshold, weatherImpactsTotals, weatherSensitivity, trustTeamRatings, paceAffectsTotals, fadeBackToBacks, weightRecentForm, rideHotStreaks, fadeColdStreaks, trustAtsTrends, regressLuck, homeCourtBoost, upsetAlert. Insight keypaths: bettingPhilosophy, perceivedEdges, avoidSituations, targetSituations.
- **States**: No skeleton — `.task(id: agentId)` hydrates instantly from `initialAgent` (if given), then ALWAYS re-fetches snapshot and re-hydrates; `hydrate(from:)` is a no-op once `hasChanges` (fresh fetch may replace a stale initialAgent but never clobbers in-progress edits). `hydrate` sets: name, emoji, spriteIndex (stored override else legacy hash-derived — `agent.spriteIndex`), color, sports, personality, customInsights, autoGenerate, time, timezone, isPublic, then `hasChanges = false`. Saving state = toolbar `ProgressView`. Save failure → error alert with `store.lastGenerationError ?? "Failed to save settings."`.
- **Interactions**: All form edits (see above); Save → `save()`: builds `[String: AnyEncodable]` payload keys `name, avatar_emoji, sprite_index, avatar_color, preferred_sports` (rawValue array), `personality_params, custom_insights, auto_generate, auto_generate_time, auto_generate_timezone, is_public` → `store.saveSettings(payload:)`; success → `hasChanges = false` + `dismiss()`. Delete button → `deleteConfirm = true`; confirm → `store.delete()`; success → `dismiss()`. "Preferred time" row → `showTimePicker = true`.
- **Sheets/dialogs/alerts**: `.sheet($showTimePicker)` → `TimePickerModal(isPresented:, time:, timezone:)` with pass-through bindings that also flip `hasChanges`. `.alert("Delete Agent?", isPresented: $deleteConfirm)`: Cancel / destructive Delete; message `Are you sure you want to delete "{name}"? This cannot be undone.` `.alert("Error", errorAlertBinding, presenting: errorMessage)` OK.
- **Animations / haptics**: None notable (stock Form behavior; conditional slider rows appear/disappear with default Form animation).
- **Gating**: Owner-only by construction (only reachable from owner surfaces). Premium: autopilot toggle-on gated on `canUseAutopilot`; public toggle-on gated on `canCreatePublicAgent`; both toggles disabled (not just intercepted) when unentitled.
- **Routes pushed**: None (dismisses back on save/delete).
- **Sub-views**: none defined in-file (uses shared `SliderInput`, `ToggleInput`, `OddsInput`, `TimePickerModal`, `PixelSpriteAvatar`).

### `AgentStatsView.swift` (~583 lines)
- **Purpose & nav context**: Admin/hidden "Platform Statistics" — whole-population agent performance distributions. Lives behind Secret Settings (pushed from `SecretSettingsView`), NOT part of `AgentsRoute`; receives `store: PlatformStatsStore` as a plain `let` prop (parent-owned). `.navigationTitle("Agent Stats")` inline, background `Color.appSurface.ignoresSafeArea()`.
- **Layout**: ScrollView → VStack(spacing 16, padding 16), sections IN ORDER: (1) `intro` card — Label "Platform Statistics" (16 heavy) with `chart.bar.xaxis` icon in `appPrimary` + explainer copy ("Win-rate distribution across every agent on the platform. Drag the threshold to filter out tiny-sample agents, or tap a bar to see who's in it.", 13pt appTextSecondary); all cards = padding 14–16 on `Color.appSurfaceElevated` in 16pt-radius continuous rounded rect. (2) `summaryCards` — 2×2 grid of stat cards (uppercased 11-semibold label over 24-heavy value): winRate metric → Mean Win % / Std Dev / "Above 52.4%" (green `appWin` when ≥50%) / Median; netUnits → Avg Units (appWin/appLoss by sign) / Std Dev ("x.xu") / Profitable (green when ≥50%) / Median (signed color); all "—" when no fit. (3) `controlBar` — metric pills from `StatMetric.allCases` (Win Rate / Net Units; pill = 12-semibold text, h14/v8, active: `appPrimary.opacity(0.18)` fill + 1pt appPrimary stroke + appPrimary text; inactive: `appBorder.opacity(0.5)` stroke + appTextSecondary), trailing `granularityMenu` (Menu labeled `slider.horizontal.3` + "Bins: Fine/Medium/Coarse", 12-semibold, `.liquidGlassBackground(in: Capsule())`, checkmark on current); second row (winRate only — hidden for net units since per-sport net units unavailable): horizontal scroll of sport pills All/MLB/NBA/NCAAB/NFL. (4) `thresholdSlider` card — "Minimum settled picks" + "≥ N" (13 heavy monospaced appPrimary), `Slider(0...100, step 1)` tint appPrimary, caption "Raising this filters out tiny-sample agents — the 0% / 100% spikes collapse and the curve tightens toward true skill." (5) `heroCard` — title `heroTitle` ("Win Rate · All Sports" / "Win Rate · {sport}" / "Net Units · All Sports") + trailing "μ x · σ y" monospaced fit summary; body: `<2` values → `lowDataPlaceholder(height: 220)` else `DistributionHistogramChart(buckets:curve:fit:domain:metric:accent: .appAccentBlue, height: 220)` with bar-tap closure + hint "Tap a bar to see the top agents in it". (6) `perSportSection` "By Sport" (18 heavy) + note "Win rate only — net units isn't tracked per sport."; one `sportCard` per real sport — MLB (accent `.appAccentBlue`), NBA (`.appWin`), NCAAB (`Color(hex: 0xF97316)`) — each: title + μ/σ, `DistributionHistogramChart` height 150 over domain 0.2...0.85, binWidth 0.05, requires ≥15 agents (`sportFloor`) else `lowDataPlaceholder(height: 100)`; then `nflEstimateCard` — "NFL" + purple "EST." badge (9 heavy, `Color(hex: 0x9B59B6)` on 0.18 capsule) + "μ x · projected"; chart with empty buckets + synthetic curve, purple accent, `showReferenceLines: true`, caption "NFL picks aren't graded yet — this is a projection (strongest sport + 5 pts), not observed data." (7) `overlaySection` "Sports Compared" — `FittedCurveOverlayChart(series:, domain: 0.2...0.85, height: 220)` with one normalized curve per qualifying real sport + always the "NFL (Est.)" purple series flagged `isEstimated: true`; `lowDataPlaceholder(120)` unless >1 series. (8) `freshnessFooter` — "Updated {relative}" (11pt appTextMuted) from `store.lastCalculatedAt`, hidden when nil. Product constraints baked in: never displays total-N population; break-even constant 0.5238.
- **Stores & bindings**: `let store: PlatformStatsStore` — reads `.loadState`, `.data` (rows expose `.decided`, `.decided(forSport:)`, `.winRate`, `.winRate(forSport:)`, `.netUnits`), `.lastCalculatedAt`; calls `.refresh()`. Statistics helpers: `DistributionStatistics.histogram/fit/curvePoints/normalizedCurvePoints`, `NormalFit(mean:sd:count:isEstimated:)`. `@State`: `metric: StatMetric = .winRate`, `sport: SportOption = .all`, `minDecided: Double = 20`, `granularity: BinGranularity = .medium`, `drill: DrillContext?`. Bin widths: winRate .02/.05/.10, netUnits 1/2/5 for fine/medium/coarse. Hero domain: winRate fixed 0...1; netUnits clamped to ±25, min/max rounded to 5s (fallback -10...10).
- **States**: `.idle`/`.loading` → `AgentStatsSkeleton()`; `.failed(message)` → `ContentUnavailableView` "Couldn't load stats" (chart.bar.xaxis) + message + `.borderedProminent` appPrimary "Retry" → `store.refresh()`; `.loaded` → `loadedBody`. `.task { if store.data.isEmpty { await store.refresh() } }`. In-body low-data: `lowDataPlaceholder` = `chart.bar.xaxis.ascending` 26 light + "Not enough data at this threshold" (12pt), min-height parameterized.
- **Interactions**: Metric pill tap → `withAnimation(.easeInOut(0.2))` metric switch; sport pill tap → same animated switch; granularity menu selection → animated; threshold Slider drag → live recompute of every cohort (`heroValues()` filters `decided >= n`; per-sport cohorts filter `decided(forSport:) >= n`); histogram bar tap (hero and per-sport cards) → sets `drill = DrillContext(title: binTitle, metric:, sport: (winRate ? sport.agentSport : nil / per-card sport), lower:, upper:)`; `binTitle` formats "62–67% Win Rate" or "+0 to +2u". Pull-to-refresh on the loaded scroll → `store.refresh()`. NFL estimate recomputes live from threshold: mean = min(0.9, strongest real-sport mean + 0.05) (fallback base 0.555), sd = mean of real sports' SDs (fallback 0.07), `isEstimated: true`.
- **Sheets/dialogs/alerts**: `.sheet(item: $drill)` → `BinAgentsSheet(title:metric:sport:lower:upper:minDecided: Int(minDecided))` (drill into top public agents in the tapped bin + their open picks). No alerts.
- **Animations & haptics**: `.easeInOut(0.2)` on metric/sport/granularity switches; `.sensoryFeedback(.selection, trigger: metric)` and `(.selection, trigger: sport)` on the control bar. No appear choreography.
- **Routes pushed**: None (leaf; sheet only).
- **Sub-types in file**: `SportOption` (enum: all/mlb/nba/ncaab/nfl; `.key` nil for all; `.agentSport` mapping), `BinGranularity` (fine/medium/coarse + `width(for:)`), `DrillContext` (Identifiable sheet item), private `SportSpec` (key/label/color), private `StatCardData`. Gating: implicitly admin/secret (parent placement); no premium checks inside.

---

## Cross-file parity notes
- The green brand accent hex `0x00E676` recurs everywhere as the default agent tint / CTA color.
- `AgentEntitlementsStore` is always a per-render facade over `ProAccessStore` (never a lifecycle-owned store).
- The unread-picks contract is a device-local UserDefaults ledger (`AgentPicksSeenStore`, keyed on agentId + latest-activity timestamp) written by the detail screen and read by the list.
- Both detail screens share identical `canSeePicks = canViewAgentPicks || isOwner` gating, `historyReloadKey` invalidation formula, `sectionGap` 28, hero 196→60 collapse, and the `AgentSectionHeader`/`AgentPickPayloadAuditSheet` components defined in `AgentDetailView.swift`.
