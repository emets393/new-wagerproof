package com.wagerproof.app.features.search

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.wagerproof.app.features.shared.TodoScreen

/** Search tab root. iOS `Features/Search/SearchView`. Placeholder — replace in place. */
@Composable
fun SearchScreen(modifier: Modifier = Modifier) {
    TodoScreen(screenName = "Search", modifier = modifier)
}
