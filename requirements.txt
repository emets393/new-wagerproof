# Python deps for the new-wagerproof model pipeline (research/nfl-extreme-outcomes/,
# research/cfb-model/). Render installs these per render.yaml; locally, `pip install -r
# requirements.txt` covers the runners (fetch -> build -> .pkl predict -> slate -> grade).
numpy
pandas
scipy
scikit-learn
requests
pyarrow
openpyxl
psycopg2-binary   # grade_week.sh runs the grading RPCs over DATABASE_URL (no psql binary needed)
